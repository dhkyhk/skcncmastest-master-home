<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.employeemapper">
    <select id="selectone" parameterType="skcnc.stockcore.framework.database.MetaHashMap" resultType="skcnc.stockcore.framework.database.MetaHashMap">
        <![CDATA[
        select id
             , password
          from employee
         where id = #{id}
        ]]>
    </select>
    <select id="selectarray" parameterType="skcnc.stockcore.framework.database.MetaHashMap" resultType="skcnc.stockcore.framework.database.MetaHashMap">
        <![CDATA[
        select id
             , password
          from employee
        ]]>
    </select>
</mapper>